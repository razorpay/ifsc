/*
constants.go is autogenerated from constants.template using data from banknames.json
Run make generate-constants to update
*/
package ifsc

const ({{ range $key, $value := . }}
        {{ $key }} = "{{ $key }}"{{ end }}
)

var bankCodes = map[string]bool{ {{ range $key, $value := . }}
        "{{ $key }}": true,{{ end }}
}

var bankName = map[string]string{ {{ range $key, $value := . }}
        "{{ $key }}": "{{ $value }}",{{ end }}
}
