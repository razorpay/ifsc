/*
constants.go is autogenerated from constants.template using data from banknames.json
Run make generate-constants to update
*/
package ifsc

const ({{ range  .Value }}
        {{ . }} = "{{ . }}"{{ end }}
)

var bankCodes = map[string]bool{ {{ range .Value }}
        "{{ . }}" : true, {{ end }}
}
